// frontend/postcss.config.js
export default {
    plugins: {
        // Tailwind CSS plugin for PostCSS
        tailwindcss: {},
        // Autoprefixer plugin for PostCSS
        // It automatically adds vendor prefixes to CSS rules
        // (e.g., -webkit-, -moz-) for cross-browser compatibility.
        autoprefixer: {},
    },
};